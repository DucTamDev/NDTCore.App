# react-native-web's peer react-dom range resolves to a version requiring
# react@^19.2.8, which conflicts with this project's pinned react@19.2.3.
# Without this, a plain `npm install` fails with ERESOLVE.
legacy-peer-deps=true
